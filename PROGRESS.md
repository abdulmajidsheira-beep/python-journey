# PROGRESS.md — سجل تقدمي في مسار AI Engineer

## الهدف
Applied AI Engineer (LLM/RAG/Agents) خلال 14 شهرًا، مع أتمتة n8n كدخل موازٍ.
المصدر الكامل للخطة: `AI-Engineer-Blueprint.md` و`Learning-Order.md` في مشروع Claude.

---

## المرحلة 0 — الأدوات: ✅ مكتملة

### Git الأساسي
- تثبيت Git على Windows، `git --version` للتحقق
- تعريف الهوية: `git config --global user.name/email`
- الدورة الكاملة: `mkdir → git init → git add → git commit → git remote add → git push`
- فهم الفرق بين commit (حفظ محلي) و push (نشر بعيد)
- فهم Staging Area ولماذا `git add` خطوة منفصلة عن `git commit`

### الفروع والدمج
- `git checkout -b` لإنشاء فرع جديد والانتقال إليه
- تجربة **merge conflict** كاملة: إنشاء فرع، تعديل نفس السطر في فرعين، محاولة الدمج، الفشل المتوقع، الحل اليدوي عبر VS Code (Accept Both Changes)، إتمام الدمج بـ `git add` + `git commit`
- فهم دقيق: التعارض يحدث عند تعديل **نفس السطر** من **فرعين مختلفين** — لا أي تعديلين عشوائيين

### .gitignore
- إنشاء `.gitignore` لمنع رفع ملفات حساسة (جُرِّب بملف `secret.txt` وهمي)
- `git status --ignored` للتحقق من الملفات المتجاهَلة

### git revert
- الفرق بين `revert` (يضيف commit جديد يُلغي تأثير commit سابق، بلا حذف من التاريخ) و`reset` (يحذف فعليًا — لم نُجرّبه بعد، خطر على العمل الجماعي)
- التعامل مع محرر Vim العالق: `Esc` ثم `:wq`، أو تجاوزه بـ `--no-edit`

### قراءة الأوامر والتوثيق
- الفرق بين خيار بشرطة واحدة وحرف (`-m`) وخيار بشرطتين وكلمة كاملة (`--message`)، وأن بعض الخيارات لا تملك اختصارًا قصيرًا (مثل `--oneline`)
- تدرّبت على قراءة `git log --help` مباشرة من التوثيق الرسمي كتمرين قراءة إنجليزية تقنية

### Linux (WSL Ubuntu)
- تثبيت WSL، اسم المستخدم `m-x-2`
- أوامر أساسية: `ls, mkdir, cd, pwd, rm/rm -r, touch, echo, cat, cp, mv, file, find, chmod +x, ps, kill`
- ملف مرجعي محفوظ: `linux-commands-day1.md`

### أدوات أخرى
- VS Code مثبَّت ومُستخدَم فعليًا (حل التعارضات بصريًا)
- حساب GitHub: `abdulmajidsheira-beep`
- المستودع الرئيسي النشط: **`python-journey`** (وليس `-AI_Engineer_course` الذي تُرك جانبًا)

---

## نظام الإنجليزية التقنية

- بُني برومبت مخصص (مع Grok، لأنه ينتج ملفات MP3 فعليًا):
  - يستخرج 30 كلمة من مادة مُعطاة (60% عامة + 40% تقنية)
  - صوت متواصل بلا تفاعل (يُستمع أثناء القيادة، بلا أسئلة أو اختبارات)
  - جملة تتكرر مرتين في الصوت فقط
  - نص مكتوب منفصل للمراجعة في المنزل
- الخطة: الاستماع في السيارة (تعويد الأذن) + القراءة والمراجعة في المنزل (الترسيخ الفعلي)
- التوصية: إضافة الكلمات لاحقًا في Anki عبر `ankiweb.net` (نسخة الويب المجانية، وليس تطبيق iOS المدفوع)

---

## القرار الحالي

**نبدأ الآن في المرحلة 1 — Python الهندسي، بالتحديد OOP (البند 9 وما بعده في `Learning-Order.md`)**

- المستخدم لديه خبرة سابقة في كتابة `class` بسيطة (غير مكتملة)
- الخطوة التالية: تقييم مستواه الفعلي في OOP، ثم البدء بمفهوم الكلاسات من الصفر بمنهجية "ارسم على ورق قبل الكود"

---

## كيفية استخدام هذا الملف

في أي محادثة جديدة مع Claude داخل هذا المشروع، الصق هذا الملف أو اطلب: **"أين توقفنا؟"** — الذاكرة التلقائية تحتفظ بالسياق، لكن هذا الملف مرجع مضمون 100% ومستقل عنها.

**حدّثه أنت بنفسك بعد كل جلسة** — بضعة أسطر تكفي: ماذا أنجزت اليوم، أين توقفت، ما التالي.
